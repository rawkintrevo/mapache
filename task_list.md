# pi-web-ui sequential task list

Goal: integrate pi-web-ui into the sole pi-chrome runner, with one active runner
per workspace, and preserve the HubSpot Chrome session's files and Pi history.

Status: implementation in progress; Tasks 1–26 are complete. These are local
issue specifications, not GitHub issue numbers. Each link contains the task's
starting files, implementation steps, acceptance criteria, validation, and scope limits.

## Start here

- [Execution guide and suggested implementation-agent goal](docs/plans/pi-web-ui-tasks/README.md)
- [Fixed product decisions and selected engineering defaults](docs/plans/pi-web-ui-tasks/decisions.md)
- [Shared interfaces, storage, auth, and lifecycle contracts](docs/plans/pi-web-ui-tasks/contracts.md)
- [Architecture overview](docs/plans/pi-web-ui-integration.md)

Execute the first unchecked task, in numeric order. All earlier tasks are prerequisites.
For a whole-checklist goal, verify and commit each task, then continue automatically.
For a single `next task` request, complete one task. Never mark failed or blocked work
complete, skip a prerequisite, or invent a missing source ID/credential. Follow the
linked stop rules. No further product decisions are queued; missing external access
or contradictory evidence still requires an explicit stop.

Difficulty: **easy** means inspection or a small focused change; **medium** means a
bounded implementation with the design already specified and meaningful tests.
These are not timing guarantees. Split oversized work into local lettered steps
within its task file while keeping the root task unchecked.

## Ordered checklist

- [x] 1. **[Inventory the deployment and identify the HubSpot source](docs/plans/pi-web-ui-tasks/01-inventory-and-protect-source.md)** - easy
  - Completed: 2026-09-11 — identified exactly one HubSpot source mapping (`rFNToErGqhiRT5Twt8vx` / `Q2ApeoyTMGQ7q26MNbhn`), recorded production Functions/Hosting/Cloud Run/image state, verified the seven source-session JSONL files (2,485 valid records), and created ignored recovery targets. No deployment, runner stop, or data copy performed. Evidence: `artifacts/qa/pi-web-ui/task-1/README.md`; restricted inventory: `artifacts/migrations/pi-web-ui/hubspot/2026-09-11-inventory.md`. Task 2 must reconcile the stored runner digest/runtime failure before cutover.
- [x] 2. **[Add the pinned upstream build and patch manifest](docs/plans/pi-web-ui-tasks/02-pin-upstream-build.md)** - medium
  - Completed: 2026-09-11 — added `session-runner/upstream/pi-web-ui/manifest.json`, ordered strict patching, reproducible source-fetch/build helper, and the pinned `pi-chrome` Docker integration. Verified the requested upstream commit/package (`46880b3` / `0.79.0`), Pi SDK `0.84.4`, adapter `2.32.1`, generated server/web output, LICENSE, and safe build descriptor. Upstream typecheck and 636 tests passed; helper negative tests passed; local Docker build and Chrome smoke passed. No cloud runner launch or deployment performed. Handoff: Task 3 can use `/opt/mapache/pi-web-ui`; existing Cloud Run services still need a later revision.
- [x] 3. **[Configure the managed app for the agent subpath](docs/plans/pi-web-ui-tasks/03-managed-embedded-build.md)** - medium
  - Completed: 2026-09-11 — added the pinned `/agent/` Vite base, fixed the favicon root-relative exception, preserved the existing `appUrl` API/WS/theme/plugin/file-preview mappings, removed service-worker registration with exact app-scope cleanup, and forced managed instances to the Pi engine while refusing self-update/runtime-install/plugin-catalog protocol messages. Upstream typecheck and 636 tests passed; generated bundle inspection, local HTTP load, managed health, and direct WebSocket refusal checks passed. Evidence: `artifacts/qa/pi-web-ui/task-3/README.md`. No Cloud Run service was launched, stopped, or deployed; Tasks 4–6 own the supervised process and public gateway mapping.
- [x] 4. **[Launch pi-web-ui as a supervised runner child](docs/plans/pi-web-ui-tasks/04-launch-upstream-process.md)** - medium
  - Completed: 2026-09-11 — added the marked-runtime config/state contract and `session-runner/lib/piWebUiProcess.js`, which starts exactly one loopback-only pinned child after restore/materialization, waits for authenticated local health, and performs bounded SIGTERM/SIGKILL shutdown without respawn or token logging. Marked runners disable the legacy Pi PTY/TUI, Chat, Goals bridge/RPC, and package declaration path; unmarked sessions retain existing behavior. Focused and aggregate session-runner tests passed (263 before the final Goals guard, then the focused guard test), lint and `npm run docs:check` passed, and local `Dockerfile.pi-chrome` build/Chrome smoke plus in-container managed health/stop smoke passed. Evidence: `artifacts/qa/pi-web-ui/task-4/README.md`. No Cloud Run service was launched or deployed. Task 5 should use the fixed internal `127.0.0.1:8787` process and marker/state paths; no storage or provisioning changes were made.
- [x] 5. **[Add the authenticated HTTP gateway for the embedded app](docs/plans/pi-web-ui-tasks/05-http-gateway.md)** - medium
  - Completed: 2026-09-11 — added the authenticated `/agent` HTTP gateway before the runner JSON parser, with signed session/audience/generation verification, scoped bootstrap cookie, private-token injection, exact-Origin mutation checks, traversal and redirect fencing, upstream-cookie filtering, streamed bodies, Range preservation, and a 10 MiB request bound based on the pinned upstream Express limit. Focused gateway/access tests passed (9), aggregate runner tests passed (269), lint and `npm run docs:check` passed. Evidence: `artifacts/qa/pi-web-ui/task-5/README.md`. No Cloud Run service was launched or deployed. Task 6 owns WebSocket forwarding.
- [x] 6. **[Proxy agent WebSockets alongside existing runner sockets](docs/plans/pi-web-ui-tasks/06-websocket-gateway.md)** - medium
  - Completed: 2026-09-11 — added `session-runner/lib/agentWebSocketGateway.js` and routed `/agent/ws` through the central noServer dispatcher to the fixed loopback `/ws`, with signed session/audience/generation/expiry and exact-Origin checks before upstream connection, private-token-only headers, query/cookie stripping, bounded message backpressure, paired close handling, and expiry fencing. Fake-upstream hello/ready/snapshot, terminal/metrics coexistence, rejection, and expiry tests passed; aggregate runner tests passed (271), lint and `npm run docs:check` passed. Evidence: `artifacts/qa/pi-web-ui/task-6/README.md`. No Cloud Run service was launched or deployed. Task 7 owns signed access API issuance.
- [x] 7. **[Expose gated signed agent access URLs from Functions](docs/plans/pi-web-ui-tasks/07-signed-agent-access-api.md)** - medium
  - Completed: 2026-09-11 — added server-owned `pi-web-ui-v1` workspace marking with an exact-target, owner-verified deployment helper; marked sessions force the curated `pi-chrome` image, propagate explicit runtime-generation metadata, and expose `agentUrl` only for owned running compatible sessions. Agent tokens use the existing browser secret/TTL with `aud: "agent"`, session ID, and generation; unmarked, unsupported, cross-owner, and missing-generation responses omit the URL. Client payloads cannot select the marker, image, or generation. Focused agent-runtime, access, session-creation, Cloud Run, and marker-helper tests passed; full Functions tests and lint passed; `npm run docs:check` checked 81 files successfully. Deployed with `firebase deploy --only functions --project pi-agents-cloud`: 6 Functions updated successfully, 0 errored; unrelated remote Functions were preserved. No QA workspace was marked because no explicit target was supplied. Evidence: `artifacts/qa/pi-web-ui/task-7/README.md`. Task 8 owns the embedded canvas and browser-side renewal.
- [x] 8. **[Embed the agent canvas and renew access without reload](docs/plans/pi-web-ui-tasks/08-iframe-and-renewal.md)** - medium
  - Completed: 2026-09-11 — added `PiWebUiCanvas` with a stable per-session iframe, Agent/Chrome/Preview tab persistence, loading/unavailable/access-error states, and a new-tab fallback. Added the pinned upstream `mapache.agent.*` postMessage bridge with exact origin/source validation, signed URL cookie renewal, expiry-triggered parent refresh, and existing WebSocket reconnect without iframe reload or prompt replay. Agent access URL updates are delivered by message while the iframe `src` stays fixed; session selection uses a keyed canvas so stale updates cannot cross sessions. Mapache frontend tests passed (44 files, 157 tests), root build and community build passed, upstream typecheck passed, upstream tests passed (70 files, 639 tests), upstream lint passed with three pre-existing warnings, fresh pinned patch verification passed, and `npm run docs:check` checked 81 files successfully. No deployment was required for this frontend/upstream bundle task. Evidence: `artifacts/qa/pi-web-ui/task-8/README.md`. Task 9 owns Mapache credential mutation enforcement.
- [x] 9. **[Keep credential mutations in Mapache](docs/plans/pi-web-ui-tasks/09-credential-ownership.md)** - medium
  - Completed: 2026-09-11 — `workspaceAuth.service.js` now treats Firestore plus session selection as authoritative for managed Pi, materializes selected credentials into `/var/lib/mapache/agent/pi/auth.json` after restore and before the supervised child, removes stale `provider-keys.json`, refuses to import restored legacy auth, and exposes a path-only capture-excluded secret inventory (`agent-auth`, `pi-provider-keys`, `pi-model-config`, `pi-mcp-oauth`, `github-cli-hosts`, and managed `legacy-pi-auth`). The pinned upstream managed server rejects provider-key/OAuth/secret-header mutations while retaining metadata-only model configuration and selection; the UI explains Mapache ownership and hides credential controls. Runner tests passed (273), upstream typecheck/tests/build passed (70 files, 641 tests), lint and `npm run docs:check` passed, and fresh patch verification passed. Evidence: `artifacts/qa/pi-web-ui/task-9/README.md`. No deployment was required because Functions code was unchanged. Task 10 should preserve this ownership boundary while connecting the single `pi-mcp-adapter` path through SDK discovery; use the existing `/workspace/.mcp.json` and Mapache token-refresh materialization contracts without duplicating credentials.
- [x] 10. **[Connect the existing MCP adapter to the SDK runtime](docs/plans/pi-web-ui-tasks/10-single-mcp-path.md)** - medium
  - Completed: 2026-09-11 — pinned `pi-mcp-adapter@2.32.1` is installed and patched into the Pi images, passed through `PI_WEB_MCP_ADAPTER_PATH`, and discovered exactly once by the managed upstream Pi SDK. The managed server skips its independent MCP bridge; adapter setup/editor, auth actions, project enable/disable, config writes, and bearer-token writes are refused while stdio/remote execution and Mapache’s Google `bearer_env` refresh path remain available. Unsupported adapter versions fail closed. The deterministic SDK fixture found one tool, executed one call, and recorded one server process; runner tests passed (276), upstream typecheck/tests/build passed (70 files, 641 tests), the root build/lint/docs checks passed, and the local `pi-chrome` image plus managed start/health/stop smoke passed. Evidence: `artifacts/qa/pi-web-ui/task-10/README.md`. No real HubSpot writes, hosted connector mutation, cloud runner launch, or deployment was performed. Task 11 should bind this single adapter-backed MCP config to workspace/history ownership without introducing a second connector path.
- [x] 11. **[Bind upstream projects and history to one workspace](docs/plans/pi-web-ui-tasks/11-workspace-binding.md)** - medium
  - Completed: 2026-09-11 — added the ordered upstream workspace/history patch and fixed managed pi-web-ui to `/workspace`, with an explicit flat `PI_CODING_AGENT_SESSION_DIR` passed to every SDK session create/continue/list/open operation. Project selection, path completion, folder creation, stale client-state restore, and history open/rename/delete now follow canonical paths and reject foreign roots or symlink escapes; the ordinary shell/PTY remains unrestricted. Client IDs retain display state only, so independent clients share transcript IDs and branches. The patch manifest now includes 0006 and 0007; pristine ordered verification and full build passed. Upstream typecheck/build passed, 71 files and 644 tests passed, session-runner tests passed (276 with 1 skip), session-runner lint, root build, and `npm run docs:check` passed (81 files). Evidence: `artifacts/qa/pi-web-ui/task-11/README.md`. No deployment, hosted runner launch, or workspace data mutation was performed. Task 12 can serialize runner-side workspace start requests against this fixed history root.
- [x] 12. **[Serialize workspace Start requests](docs/plans/pi-web-ui-tasks/12-workspace-start-reservation.md)** - medium
  - Completed: 2026-09-11 — added marked-workspace runtime reservation fields and a generation-matched state seam on top of the existing sync-writer/Chrome transaction. Equal operation IDs converge on one session without overwriting it; different operations are rejected with `agent_runtime_workspace_busy` while the marked session is provisioning, running, restarting, resizing, stopping, or deleting. Failed attempts release the current reservation, preserve generation history, and allow the next operation to increment it; unmarked workspaces retain the legacy path. Focused reservation race/idempotency tests, all Functions tests and lint, root build, and `npm run docs:check` passed. Targeted deployment updated six local Functions successfully (API revision `api-00192-yes`, provisioning trigger `provisionqueuedsession-00047-hut`) with the required `mapache-api@pi-agents-cloud.iam.gserviceaccount.com`; remote-only Functions were preserved after a full-deploy deletion guard. Evidence: `artifacts/qa/pi-web-ui/task-12/README.md`. Contracts now define the exact fields; Task 13 owns boot-instance fencing.
- [x] 13. **[Fence duplicate and stale runner instances](docs/plans/pi-web-ui-tasks/13-boot-instance-fencing.md)** - medium
  - Completed: 2026-09-11 — added `session-runner/lib/workspaceAuthority.js` and integrated per-boot Firestore authority with the reserved workspace generation. Duplicate boots fail closed; controlled stop/recreate clears the predecessor before generation handoff; renew/assert mismatches or unavailable coordination revoke local admission, reject state-changing HTTP/WS work and file publication, and terminate the detached pi-web-ui process group. Existing sync-writer cleanup now clears boot ownership only for a matching generation/boot ID. Deterministic A/B, revoked-generation, delayed-callback, coordination-loss, HTTP/WS, lifecycle-order, and process-group tests passed. Session-runner tests passed (281, 1 existing skip), Functions lint/tests passed, root build passed, and `npm run docs:check` passed (81 files). Targeted deployment updated six local Functions successfully (API revision `api-00193-nub`, provisioning revision `provisionqueuedsession-00048-bik`) with the required `mapache-api@pi-agents-cloud.iam.gserviceaccount.com`; remote-only Functions were preserved. Evidence: `artifacts/qa/pi-web-ui/task-13/README.md`. Physical container liveness remains distinct from admitted writer ownership; Task 14 can include the generation/boot identity in snapshot manifests.
- [x] 14. **[Capture versioned agent-state snapshots](docs/plans/pi-web-ui-tasks/14-capture-agent-state.md)** - medium
  - Completed: 2026-09-11 — added `session-runner/lib/agentSnapshot.service.js` with local staging for complete flat Pi JSONL history, allowlisted non-secret Pi settings, upstream UI state, and only history-referenced uploads. Manifest version 1 records the exact `{workspacePrefix}/{internalStorageDir}/agent-snapshots/v1` namespace, workspace/session/generation/boot identity, capture time, relative paths, kinds, byte lengths, SHA-256 checksums, and safe permission metadata. Stable JSON/settings are parsed and rechecked; incomplete trailing JSONL is marked and interior malformed records, changed sources, traversal, unsafe symlinks, credential material, caches, locks, and process state are rejected or excluded. Deterministic snapshot fixtures passed, including attachment references, permissions, exclusions, safe symlinks, and concurrent replacement detection. Session-runner lint passed, full runner tests passed (287, 1 existing skip), and `npm run docs:check` passed (81 files). No Functions change, deployment, Cloud Storage write, or checkpoint-pointer mutation was made. Evidence: `artifacts/qa/pi-web-ui/task-14/README.md`. Task 15 can upload these staging files to immutable objects and transactionally publish the pointer.
- [x] 15. **[Publish checkpoints without stale-writer overwrite](docs/plans/pi-web-ui-tasks/15-publish-checkpoints.md)** - medium
  - Completed: 2026-09-11 — added `session-runner/lib/agentCheckpoint.service.js` to upload Task 14 captures as immutable, generation/boot-scoped objects and publish `agentRuntimeCheckpoint` only in an authority-checked Firestore transaction. Marked-runtime workspace sync now publishes versioned content manifests with SHA-256 metadata and tombstones; stale bases reject without mutable deletes, while unmarked workspaces retain the legacy writer. Safe `lastCheckpointAt`/`checkpointError` fields are exposed through `/healthz`. Focused checkpoint tests passed (5), route/status tests passed, and the full runner suite, lint, and docs check passed. No Functions changes or deployment were required. Evidence: `artifacts/qa/pi-web-ui/task-15/README.md`. Task 16 can consume the published workspace-file and agent-state manifests during restore.
- [x] 16. **[Restore saved state before starting the agent](docs/plans/pi-web-ui-tasks/16-restore-before-start.md)** - medium
  - Completed: 2026-09-11 — added `agentCheckpointRestore.service.js` to read only published workspace/agent manifests, validate pointer identity, object checksums, JSON/JSONL contents, safe paths, and symlink targets in staging, then install managed roots with rollback before writer admission, auth materialization, and the supervised child. Marked workspace sync no longer reads the legacy flat prefix and `.git` remains archive-backed; missing pointers leave new runtimes empty/default, while corrupt, missing, unsafe, or mixed-generation checkpoints fail startup without partial replacement. Restore fixtures covered nested/hidden files, history, settings, UI state, referenced attachments, tombstones, symlinks, corruption, missing objects, and mixed generations; lifecycle ordering and no-legacy-fallback tests passed. Session-runner tests passed (298 total, 297 pass, 1 existing skip), focused restore/workspace/lifecycle tests passed (13), lint and `npm run docs:check` passed. No Functions, image, deployment, or Cloud Storage publication changes were made. Evidence: `artifacts/qa/pi-web-ui/task-16/README.md`. Task 17 can add paused native Goal restoration on top of the pre-agent restore boundary.
- [x] 17. **[Restore native Goal information in a paused state](docs/plans/pi-web-ui-tasks/17-persist-paused-native-goals.md)** - medium
  - Completed: 2026-09-11 — added upstream patch `0008-native-goal-persistence.patch` and manifest wiring. Native Goal display fields now persist in the captured upstream `client-state.json` UI data under stable session identity, restore as paused with fresh wizard/reviewer runtime state, and resume only after an explicit Goal-bar action; background conversation state cannot overwrite the active tab. Added the `resume_goal` protocol/dispatch path, localized paused/resume UI, all locale entries, DSH dispatch compatibility, and deterministic persistence/restart/conversation tests. Patch-chain verification passed from the pinned archive; upstream typecheck, 647 tests, and production build passed; the repository docs check passed in the Task 18 validation. Evidence: `artifacts/qa/pi-web-ui/task-17/README.md`. No Functions changes, Cloud Storage publication, image build, or deployment was required. Task 18 can add runtime quiesce without turning restored Goals into automatic jobs.
- [x] 18. **[Stop upstream agent and tool writers through a narrow control hook](docs/plans/pi-web-ui-tasks/18-quiesce-runtime.md)** - medium
  - Completed: 2026-09-11 — added upstream patch `0009-runtime-quiesce-control.patch` and manifest wiring, plus the runner adapter’s local control-socket quiesce/activity path. Managed shutdown now rejects new upstream prompts, execution, terminal writes, Goal/reviewer work, and indirect subagent writes; cancels active questions/bash/PTY/background work; pauses native Goals; waits for cooperative drain; and escalates through the existing detached process-group stop with confirmed exit. Safe activity counters remain available from `/healthz` without browser sockets. Fresh ordered patch verification, upstream typecheck/tests/build (74 files, 653 tests), focused runner tests (20), full `npm run check` (304 runner tests, 303 passed and 1 existing skip; 157 frontend tests), and `npm run docs:check` passed. No Functions changes, Cloud Storage publication, image build, or deployment was required. Evidence: `artifacts/qa/pi-web-ui/task-18/README.md`. Task 19 owns scheduled saves and the final manual-Stop checkpoint.
- [x] 19. **[Schedule saves and require a final checkpoint on manual Stop](docs/plans/pi-web-ui-tasks/19-checkpoint-and-stop.md)** - medium
  - Completed: 2026-09-11 — added the serialized checkpoint scheduler with 30-second workspace saves, 60-second agent snapshots, completed-turn debounce, bounded final publication, and safe error recording. Manual Stop now quiesces and confirms managed writers are stopped before final Chrome/agent/workspace capture; failed runner acknowledgement leaves `stop_failed` and blocks restart replacement. Cloud Run signals use a shorter best-effort save budget, with the last completed immutable checkpoint as the crash-recovery boundary. Fresh ten-patch verification, upstream typecheck/tests/build (74 files, 653 tests), session-runner tests (310 total, 309 passed and 1 existing skip), full repository check/build, and `npm run docs:check` passed. Targeted deployment updated six local Functions successfully (`api-00194-nod`, `googlemcptoken-00035-wek`, `provisionqueuedsession-00049-cey`, `reconcileworkspacesyncwriters-00048-ciq`, `refreshrunnerimagefreshness-00048-huc`, `reapidlesessions-00174-qix`) with `mapache-api@pi-agents-cloud.iam.gserviceaccount.com`; remote-only `checkpointAuthority`, `checkpointPublisher`, and `operationLedger` were preserved. Evidence: `artifacts/qa/pi-web-ui/task-19/README.md`. Task 20 owns browser-independent Cloud Run lifetime.
- [x] 20. **[Make Cloud Run lifetime independent of browser traffic](docs/plans/pi-web-ui-tasks/20-cloud-run-lifecycle.md)** - medium
  - Completed: 2026-09-11 — marked pi-web-ui Cloud Run templates now use one min/max instance and v2 instance-based CPU allocation (`resources.cpuIdle: false`), while unmarked sessions retain their existing template and browser-idle behavior. Marked sessions bypass the browser-idle reaper; explicit restart/resize requests bounded quiesce/final-save acknowledgement, confirmed service deletion, runtime-generation advancement, and only then replacement provisioning. Delete uncertainty or startup failure cannot launch a successor, and absent-service replacement preserves checkpoint recovery metadata with an interrupted warning. Focused Cloud Run/lifecycle tests passed, `npm run check` passed (310 runner tests with 309 passed and 1 existing skip; 157 frontend tests), and `npm run docs:check` passed. Targeted production deployment succeeded with six Functions updated: `api-00195-has`, `googlemcptoken-00036-wez`, `provisionqueuedsession-00050-vax`, `reconcileworkspacesyncwriters-00049-cah`, `refreshrunnerimagefreshness-00049-zav`, and `reapidlesessions-00175-goj`; all use `mapache-api@pi-agents-cloud.iam.gserviceaccount.com`. Evidence: `artifacts/qa/pi-web-ui/task-20/README.md`. Task 21 can surface these safe runtime/checkpoint failures in the session shell.
- [x] 21. **[Show runtime and persistence failures in the session shell](docs/plans/pi-web-ui-tasks/21-recovery-status-ui.md)** - medium
  - Completed: 2026-09-11 — added the marked-runtime `SessionRuntimeStatus` surface with server-reported starting/ready/stopping/stopped/error state, last successful checkpoint time, persistent checkpoint/lifecycle errors, and restart blocking while stop outcome is uncertain. Signed access renewal retains the last usable URL, surfaces actionable browser-access failures, opens the current authenticated Agent URL in a new tab, and backs off repeated refresh attempts with a bounded delay. Added status/action/access regression tests; all frontend tests passed (44 files, 161 tests), `npm run build` passed, `npm run docs:check` passed (81 files), and `git diff --check` passed. No Functions code or endpoint changed, so no deployment was required. Evidence: `artifacts/qa/pi-web-ui/task-21/README.md`. Task 22 owns the complete local vertical slice.
- [x] 22. **[Verify the complete local runtime before cloud rollout](docs/plans/pi-web-ui-tasks/22-local-vertical-slice.md)** - medium
  - Completed: 2026-09-11 — added the opt-in local `pi-web-ui` vertical slice at `session-runner/lib/piWebUi.localVertical.test.js` with a deterministic OpenAI-compatible model and stdio MCP fixture. The real pinned pi-web-ui image/server and composed Mapache HTTP/WebSocket gateway now cover streaming, MCP tool execution, file editing, history and multiple conversations, shell, native Goal/reviewer, signed access renewal, Chrome/Preview health, checkpoint upload/publication, writer fencing, quiesce, restart, and paused no-auto-resume restoration. Added an actual composed authority/checkpoint race test for concurrent admission and stale publication, plus acceptance of valid array-shaped upstream UI JSON state. The final local image `sha256:3ed2cac869375554292fdea793ea2004800a52c0e493cc20f70549ad29bb94a0` was built from pinned upstream commit `46880b3772591beac91c0c1792bdc79a6fe3671f` (package `0.79.0`, Pi SDK `0.84.4`, adapter `2.32.1`); the image smoke and opt-in vertical suite passed. `npm run check` passed: docs 81 files, runner 313 tests with 311 passed and 2 expected skips, frontend 44 files/161 tests, and both builds. Evidence: `artifacts/qa/pi-web-ui/task-22/README.md`. No cloud rollout, hosted QA, production data mutation, or deployment was performed. Task 23 owns the one-off HubSpot export tool.
- [x] 23. **[Build the one-off HubSpot export and manifest tool](docs/plans/pi-web-ui-tasks/23-one-off-export-tool.md)** - medium
  - Completed: 2026-09-11 — added the migration-specific `scripts/hubspot-pi-web-export.mjs` CLI and `scripts/hubspotPiWebExport.mjs` core. It consumes the restricted Task 1 inventory, requires explicit source IDs/prefixes and a separate output prefix, defaults to a no-write inspection plan, and requires an explicit source controller proof of quiescence plus confirmed stop before execution. The restricted export preserves hidden workspace files, `.git`, binary bytes, selected-session JSONL, and readable history-referenced attachments; records every copied/skipped path, excludes Chrome state and other sessions, rejects source/output collisions and non-quiescent captures, refuses overwrite, and flags preserved incomplete trailing JSONL. Four fixture tests and a dry-run against the actual inventory passed; `npm run check` passed (313 runner tests: 311 passed/2 expected skips, 161 frontend tests, docs/builds). Evidence: `artifacts/qa/pi-web-ui/task-23/README.md`. No live HubSpot runner was quiesced or stopped, no Cloud Storage prefix was written, and no deployment was performed. Task 24 owns import and validation.
- [x] 24. **[Import and validate HubSpot files and history](docs/plans/pi-web-ui-tasks/24-one-off-import-tool.md)** - medium
  - Completed: 2026-09-11 — added the isolated Task 23 backup verifier/importer at `scripts/migrations/pi-web-ui-hubspot/`, with no-write planning, explicit source/target identity and prefix checks, checksum/path/symlink validation, credential exclusion, fixed `/workspace`/flat sessions/Pi config/UI layout staging, and refusal to overwrite target work. The pinned Pi SDK `SessionManager.listAll`/`open` APIs validate every imported history without starting a model turn; JSONL bytes, IDs, branches, and attachments remain unchanged. Copied managed settings remove only conflicting `pi-goal-x` declarations and record intentional diffs; immutable backups are never edited, and a receipt makes reruns idempotent. Import, verify-only, collision, corruption, and real-SDK fixture tests passed (9); `npm run check` passed (313 runner tests: 311 passed/2 expected skips, 161 frontend tests, docs/builds). Evidence: `artifacts/qa/pi-web-ui/task-24/README.md`. No live source, Cloud Storage, Firestore, Cloud Run, Hosting, or Functions resource was modified or deployed. Task 25 can compose hosted QA and migration checks using an isolated rehearsal target.
- [x] 25. **[Write bounded hosted QA cases and migration checks](docs/plans/pi-web-ui-tasks/25-compose-hosted-qa.md)** - medium
  - Completed: 2026-09-11 — added reusable, opt-in Chrome DevTools manifests for an explicitly pre-marked disposable `pi-chrome` workspace, managed chat and read-only MCP/auth probes, multiple histories, terminal/files/Git, native Goal pause/resume, Chrome/Preview, access renewal, resource changes, stop/restart, duplicate-start convergence, writer fencing, checkpoint/replacement failure contracts, and no-auto-resume. Cases require existing configured providers, read-only connectors, exact recorded cleanup IDs, and a named deterministic fault harness where needed; missing dependencies are blocked rather than mocked. Added the restricted Task 23–24 migration checklist and a non-secret release evidence template. JSON/composition validation passed for 28 manifests; `npm run docs:check` passed (81 files). Hosted QA was intentionally not run; no model, CRM, source, or cloud resource was mutated. Evidence: `artifacts/qa/pi-web-ui/task-25/README.md`. Task 26 can deploy the isolated canary before Tasks 27–28 execute these cases.
- [x] 26. **[Deploy a pinned canary and preview UI](docs/plans/pi-web-ui-tasks/26-deploy-isolated-canary.md)** - medium
  - Completed: 2026-09-12 — passed the fresh local gate, built Cloud Build `8fc0442e-795e-4ed8-84f1-a132ff232a81`, and published immutable `pi-chrome-a89865ed4f18e0579df75d89b6df06bc671d02ea` at digest `sha256:686e420eaa390ac3acd8458f34ce791c59a6e5bdd1ef5d0e1877984c500c4901` without moving the production `pi-chrome` tag. Created and marked disposable workspace `UmycRCVssparzDCzpTpi`, pinned session `operation-40540ca978f7f935784ab301302fdebc` to that digest, and deployed Hosting preview `pi-web-canary-a89865ed` (`https://pi-agents-cloud--pi-web-canary-a89865ed-21aabe97.web.app`, expires 2026-09-19). Verified API revision `api-00195-has`, runner/API service accounts, 1/1 scaling, CPU throttling disabled, port 8080, signed Agent HTTP/browser-status/WebSocket access, and QA login/API access. The HubSpot Cloud Run service was already absent and was not recreated or started; per operator direction, the source of truth for the later migration is the preserved GCS workspace/session history/files under the recorded source prefix. Evidence: `artifacts/qa/pi-web-ui/task-26/README.md`. Cleanup is deferred to Task 37; Task 27 can use the recorded canary IDs.
- [x] 27. **[Run hosted functional browser QA](docs/plans/pi-web-ui-tasks/27-hosted-functional-qa.md)** - medium
  - Completed: 2026-09-12 — ran the bounded hosted browser cases against disposable canary workspace `UmycRCVssparzDCzpTpi` on immutable runner digest `sha256:686e420eaa390ac3acd8458f34ce791c59a6e5bdd1ef5d0e1877984c500c4901`, with the operator-authorized temporary read-only `qa_readonly` connector. The real hosted UI passed runner/Agent readiness, native Goal pause, inspector/auth surfaces, MCP discovery and one read-only tool call, file edit/save/download/upload, Terminal → Agent history switching, and Preview safe-not-ready reporting. The two live turns returned `MIGRATION_QA_OK` and `QA_READ_ONLY_OK`; no third turn, CRM write, HubSpot/source mutation, or credential change was performed. Fixed nested hosted file-query encoding in the frontend/API, deployed Functions revision `api-00197-zuz`, redeployed only the canary Hosting channel, and passed the focused dispatch test, `npm run build`, `npm run docs:check`, and `git diff --check`. Evidence: `artifacts/qa/pi-web-ui/task-27/README.md`, including screenshots plus console/network summaries. Cleanup remains deferred to Task 37.
- [x] 28. **[Run hosted persistence and failure-recovery QA](docs/plans/pi-web-ui-tasks/28-hosted-lifecycle-qa.md)** - medium
  - Completed: 2026-09-12 — ran the bounded hosted persistence/failure-recovery checks against the authenticated canary preview using disposable marked workspaces and immutable QA runner image `pi-chrome-qa-fault-harness-b0df25b` at digest `sha256:e6b6793d6180a13ce6cb6c3345c0e950b857901f28a0d880456c0adf670ea157` (Cloud Build `01caf13d-c704-4b43-b2fd-686a0da180be`). Duplicate starts converged to one session; short-lived access renewed while the Agent frame stayed rendered; a real inert turn survived storage-publication injection; writer revocation fenced a state-changing Agent request with `503 writer_authority_lost`; forced loss restored the checkpoint with no automatic prompt (the UI marker appeared exactly once); and uncertain replacement returned bounded `502 session_stop_failed` with no successor revision. Hosted API revision `api-00198-qog` served the QA harness. The final disposable services, sessions, and workspaces were deleted and verified absent; no source, CRM, remote repository, or production image tag was mutated. Evidence: `artifacts/qa/pi-web-ui/task-28/README.md`. Aggregate checks and docs validation passed after the hosted fixes.
- [ ] 29. **[Rehearse the one-off migration from a consistent source backup](docs/plans/pi-web-ui-tasks/29-rehearse-hubspot-migration.md)** - medium
- [ ] 30. **[Move the HubSpot workspace to the validated runner](docs/plans/pi-web-ui-tasks/30-cut-over-hubspot.md)** - medium
- [ ] 31. **[Make the embedded app the primary workspace surface](docs/plans/pi-web-ui-tasks/31-simplify-workspace-shell.md)** - medium
- [ ] 32. **[Make pi-chrome the sole backend creation path](docs/plans/pi-web-ui-tasks/32-default-pi-chrome-backend.md)** - medium
- [ ] 33. **[Remove the old Chat, Goals, and duplicate agent controls](docs/plans/pi-web-ui-tasks/33-retire-legacy-agent-controls.md)** - medium
- [ ] 34. **[Remove unsupported runner families and build paths](docs/plans/pi-web-ui-tasks/34-retire-other-runner-builds.md)** - medium
- [ ] 35. **[Validate the final code and reconcile developer documentation](docs/plans/pi-web-ui-tasks/35-final-validation-and-docs.md)** - medium
- [ ] 36. **[Publish and verify the final one-runner release](docs/plans/pi-web-ui-tasks/36-publish-final-release.md)** - medium
- [ ] 37. **[Clean up QA resources and close the implementation checklist](docs/plans/pi-web-ui-tasks/37-cleanup-and-handoff.md)** - easy

## Completion notes

Add each task's dated Completed or Blocked note immediately below its checkbox,
including verification and deployment outcome when applicable. Store sensitive
migration inventories/backups outside Git. Keep screenshots/traces in the ignored
artifact locations specified by the execution guide.

## Previous checklist

The completed developer-wiki refactor checklist is preserved in
[the historical archive](docs/prior_task_lists/developer-wiki-refactor-completed-2026-06-17.md).
It is not part of this implementation goal.
