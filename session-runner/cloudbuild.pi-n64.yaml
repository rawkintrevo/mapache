substitutions:
  _IMAGE: us-central1-docker.pkg.dev/pi-agents-cloud/pi-agents/session-runner:pi-n64

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - Dockerfile.pi-n64
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
