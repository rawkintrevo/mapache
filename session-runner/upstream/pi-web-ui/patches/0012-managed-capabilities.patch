diff --git a/server/managed.ts b/server/managed.ts
index becb07f..88a5f06 100644
--- a/server/managed.ts
+++ b/server/managed.ts
@@ -24,17 +24,8 @@
  *   software on the machine, not about what the interface can do.
  */
 
-/** The client messages that install or replace software on the machine. */
-export const MANAGED_MESSAGES = [
-	/** Update check behind the top-bar badge. */
-	"check_update",
-	/** "Re-check all" in the UPDATE panel (app + components). */
-	"check_updates_all",
-	/** Installs the pi agent itself. */
-	"install_pi_agent",
-	/** Plugin market: fetches and installs a plugin from the network. */
-	"plugin_catalog_add",
-] as const;
+/** Requests that remain unavailable in a managed deployment. */
+export const MANAGED_MESSAGES = [] as const;
 
 export type ManagedMessage = (typeof MANAGED_MESSAGES)[number];
 
diff --git a/tests/unit/managed.test.ts b/tests/unit/managed.test.ts
index 93c6483..97d5240 100644
--- a/tests/unit/managed.test.ts
+++ b/tests/unit/managed.test.ts
@@ -36,23 +36,12 @@ describe("managed instances", () => {
 		}
 	});
 
-	it("refuses self-update and network plugin installs, and says why", () => {
-		for (const type of MANAGED_MESSAGES) {
-			const why = managedRefusal(type, true);
-			expect(why, type).toBeTruthy();
-			expect(why, type).toMatch(/managed/i);
+	it("allows user-initiated updates and plugin catalog management in managed mode", () => {
+		for (const type of ["check_update", "check_updates_all", "install_pi_agent", "plugin_catalog_add"]) {
+			expect(managedRefusal(type, true), type).toBeNull();
 		}
 	});
 
-	it("covers the whole self-update surface, not just the button", () => {
-		// The badge calls check_update, the panel's "Re-check all" calls
-		// check_updates_all, and "install pi" calls install_pi_agent. Leaving one
-		// open would leave a way to reinstall around the deploy.
-		expect([...MANAGED_MESSAGES].sort()).toEqual(
-			["check_update", "check_updates_all", "install_pi_agent", "plugin_catalog_add"].sort(),
-		);
-	});
-
 	it("touches nothing else: chatting, git, terminal and plugins already there", () => {
 		for (const type of ["prompt", "scm_status", "terminal_create", "plugins_reload", "plugin_catalog_remove"]) {
 			expect(managedRefusal(type, true), type).toBeNull();
diff --git a/web/src/components/PiSetupModal.tsx b/web/src/components/PiSetupModal.tsx
index 74e32df..d94fd66 100644
--- a/web/src/components/PiSetupModal.tsx
+++ b/web/src/components/PiSetupModal.tsx
@@ -2,7 +2,7 @@ import { useEffect, useState } from "react";
 import { FiCpu, FiRefreshCw, FiX } from "react-icons/fi";
 import type { ProviderStatus } from "../types";
 import { useT } from "../i18n";
-import { appSend, useIsManaged } from "../app-globals";
+import { appSend } from "../app-globals";
 
 interface PiSetupModalProps {
 	/** Fetched from the latest snapshot; true once auth.json has credentials. */
@@ -24,8 +24,6 @@ interface PiSetupModalProps {
  */
 export function PiSetupModal({ piConfigured, piAgentInstalled, providers, installResult, onClose }: PiSetupModalProps) {
 	const t = useT();
-	// PI_WEB_MANAGED=1：装软件不是本页的事（全局，见 web/src/app-globals.ts）。
-	const managed = useIsManaged();
 	const [installing, setInstalling] = useState(false);
 	const [provider, setProvider] = useState("");
 	const [apiKey, setApiKey] = useState("");
@@ -92,14 +90,7 @@ export function PiSetupModal({ piConfigured, piAgentInstalled, providers, instal
 				</div>
 				<p className="modal-desc">{t("setupDesc")}</p>
 
-				{managed ? (
-					<div className="setup-actions">
-						<div className="setup-done">{t("credentialsManaged")}</div>
-						<button type="button" className="btn" onClick={onClose}>
-							{t("skip")}
-						</button>
-					</div>
-				) : installFailed ? (
+				{installFailed ? (
 					<div className="setup-failed">
 						<div className="setup-done">{t("installFailed")}</div>
 						<pre className="setup-detail">{installResult.detail}</pre>
diff --git a/web/src/components/TopBar.tsx b/web/src/components/TopBar.tsx
index cd5f722..a7ff39b 100644
--- a/web/src/components/TopBar.tsx
+++ b/web/src/components/TopBar.tsx
@@ -493,10 +493,7 @@ export function TopBar({
 						))}
 					</Dropdown>
 
-					{/* Managed instances omit upstream release controls; the deployment
-					    pipeline decides when the embedded runtime changes. */}
-					{!managed && (
-						<Dropdown
+					<Dropdown
 							trigger={
 								<>
 									<FiDownload />
@@ -527,11 +524,9 @@ export function TopBar({
 							<div className="dd-header">{t("update")}</div>
 							{renderUpdateBody()}
 							{renderAllUpdatesBody()}
-						</Dropdown>
-					)}
+					</Dropdown>
 
-					{!managed && (
-						<a
+					<a
 							className="chip github"
 							href="https://github.com/xing-shuyin/pi-web-ui"
 							target="_blank"
@@ -567,7 +562,7 @@ export function TopBar({
 						open={moreOpen}
 						onOpenChange={(v) => {
 							setMoreOpen(v);
-							if (v && !managed) {
+							if (v) {
 								appSend({ type: "check_update" });
 								appSend({ type: "check_updates_all" });
 							}
@@ -630,9 +625,7 @@ export function TopBar({
 								{locale === "zh" ? th.name : (th.nameEn ?? th.name)}
 							</DropdownItem>
 						))}
-						{!managed && (
-							<>
-								<div className="dd-header">{t("update")}</div>
+						<div className="dd-header">{t("update")}</div>
 								{renderUpdateBody()}
 								{renderAllUpdatesBody()}
 								<a
