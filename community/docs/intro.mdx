---
sidebar_position: 1
---

# Quick Start

Mapache Tools runs coding agents in isolated, Git-linked serverless workspaces.
Use the webapp when a project needs its own runtime image, credential set, and
terminal session without polluting your local machine.

## Start a Workspace

1. Open the Mapache Tools app.
2. Create or select a workspace.
3. Connect the Git repository you want the agent to work in.
4. Choose a session image for the project.
5. Launch the workspace session.

Each session provisions a Cloud Run backed runner with a terminal-first workflow,
workspace file sync, and browser-visible state.

## Scope Credentials

Store provider credentials in your profile, then enable only the providers a
workspace needs. Mapache writes the generated auth files and environment values
inside the workspace boundary so different projects can use different auth
surfaces safely.

## Verify Changes

Inside a session, use the terminal to run the project checks that matter for the
current change. For frontend work in this repository, run:

```bash
npm run build
```

For community site work, run:

```bash
cd community
npm run build
```
