{
  "id": "script.openSessionModal",
  "title": "Open the create session modal",
  "actions": [
    {"type": "click", "role": "button", "name": "Create session"},
    {"type": "waitForSnapshotText", "text": "New session", "timeoutMs": 5000},
    {"type": "waitForSnapshotNotText", "text": "Session type", "timeoutMs": 1000},
    {"type": "waitForSnapshotNotText", "text": "SSH target", "timeoutMs": 1000}
  ],
  "successSignals": [
    "New session modal opens with image, CPU, memory, and env fields",
    "Session type choices are absent because the selected workspace determines the session target"
  ]
}
