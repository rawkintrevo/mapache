{
  "id": "script.resizeSelectedSession",
  "title": "Resize selected session resources",
  "parameters": {
    "cpu": "1",
    "memory": "2Gi"
  },
  "actions": [
    {"type": "click", "selector": ".drawer-list-row.active button[title^='Edit ']"},
    {"type": "click", "text": "Advanced settings"},
    {"type": "select", "name": "editSessionCpu", "value": "{{cpu}}"},
    {"type": "select", "name": "editSessionMemory", "value": "{{memory}}"},
    {"type": "click", "role": "button", "name": "Save changes"},
    {"type": "waitForNetwork", "requestUrlIncludes": "/resize", "method": "POST", "expectedStatus": 200, "timeoutMs": 30000}
  ],
  "successSignals": [
    "POST /resize returns 200 and session remains selectable"
  ],
  "requires": {
    "cloudRun": "Selected session exists"
  }
}
